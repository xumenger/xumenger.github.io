---
layout: post
title: 源码面前了无秘密【Netty】：NioEventLoopGroup 与ServerBootstrap
categories: 源码面前了无秘密  
tags: netty Netty NIO Java Reactor TCP 网络编程 selector 多线程 并发编程 JVM 高并发 通信 EventLoop 事件驱动 网络模型 IO NioEventLoopGroup ServerBootstrap
---

>参考《Netty 源码剖析与应用》

基于Netty 实现一个服务端程序，有一段核心的典型代码

```java
// 新建两个线程组，Boss线程组启动一个线程，监听OP_ACCEPT事件
// Worker 线程组默认启动 CPU核数 * 2 的线程，监听客户端连接的OP_READ、OP_WRITE事件，处理IO
EventLoopGroup bossGroup = new NioEventLoopGroup(1);
EventLoopGroup workerGroup = new NioEventLoopGroup();

// ServerBootstrap 为Netty 服务启动辅助类
ServerBootstrap serverBootstrap = new ServerBootstrap();
serverBootstrap.group(bossGroup, workerGroup);
// 设置TCP Socket 通道为NioServerSocketChannel，如果是UDP，则为DatagramChannel
serverBootstrap.channel(NioServerSocketChannel.class);
serverBootstrap.option(ChannelOption.SO_BACKLOG, 128);

...
```

所以可以看到NioEventLoopGroup、ServerBootstrap 是两个核心的类，那么接下来就简单分析一下这两个核心类的源码

