---
layout: post
title: 源码面前了无秘密【Spring】：分析SpringBoot 集成tomcat 的原理
categories: 源码面前了无秘密  
tags: Java Spring SpringBoot tomcat Servlet容器 IoC容器 Bean BeanDefinition HTTP 端口 自动化配置 getBean() 依赖注入 
---

>本文的重点在于将如何调试、分析、阅读源码的过程、思路和方法整理下来，作为后续的有力参考！

一个典型的SpringBoot 的启动程序，依次来分析SpringBoot 如何集成Tomcat 这个Servlet 容器、自动化配置等的实现原理
 
```java
@SpringBootApplication
@EnableBeeScheduling
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```
 
debug 逐行跟踪！
 
如果是web 环境启动SpringBoot，对应的ApplicationContext 是org.springframework.boot.context.embedded.AnnotationConfigEmbeddedWebApplicationContext
 
它的父类是org.springframework.boot.context.embedded.EmbeddedWebApplicationContext
 
在调用ApplicationContext 的refresh() 方法的时候，进一步调用到onRefresh() 方法，其实是由EmbeddedWebApplicationContext 实现的onRefresh() 方法
 
```java
// org.springframework.boot.context.embedded.EmbeddedWebApplicationContext
 
// onRefresh() 是Spring 框架中模板设计类型的设计，这个是子类需要实现的模板方法
// EmbeddedWebApplicationContext 就实现了这个方法
@Override
protected void onRefresh() {
    super.onRefresh();
    try {
        createEmbeddedServletContainer();
    }
    catch (Throwable ex) {
        throw new ApplicationContextException("Unable to start embedded container",
                ex);
    }
}
 
 
// onRefresh() 实际是调用了createEmbeddedServletContainer() 方法，来创建Servlet 容器
private void createEmbeddedServletContainer() {
    EmbeddedServletContainer localContainer = this.embeddedServletContainer;
    ServletContext localServletContext = getServletContext();
    if (localContainer == null && localServletContext == null) {
        // 调试发现会走这一行
        EmbeddedServletContainerFactory containerFactory = getEmbeddedServletContainerFactory();
        this.embeddedServletContainer = containerFactory
                .getEmbeddedServletContainer(getSelfInitializer());
    }
    else if (localServletContext != null) {
        try {
            getSelfInitializer().onStartup(localServletContext);
        }
        catch (ServletException ex) {
            throw new ApplicationContextException("Cannot initialize servlet context",
                    ex);
        }
    }
    initPropertySources();
}
```
 
继续看EmbeddedWebApplicationContext.getSelfInitializer() 方法，创建了一个ServletContextInitializer 类型，并实现了onStartup() 方法
 
```java
/**
 * Returns the {@link ServletContextInitializer} that will be used to complete the
 * setup of this {@link WebApplicationContext}.
 * @return the self initializer
 * @see #prepareEmbeddedWebApplicationContext(ServletContext)
 */
private org.springframework.boot.web.servlet.ServletContextInitializer getSelfInitializer() {
    return new ServletContextInitializer() {
        @Override
        public void onStartup(ServletContext servletContext) throws ServletException {
            selfInitialize(servletContext);
        }
    };
}
```
 
继续回到EmbeddedWebApplicationContext.createEmbeddedServletContainer() 中调用的EmbeddedServletContainerFactory.getEmbeddedServletContainer() 方法，可以看到这里创建了Tomcat Servlet Container。以这个函数的逻辑为切入点，研究Tomcat 类、Connector 类可以帮助我们进一步理解tomcat 的架构设计，当然本文暂时不研究这个点
 
```java
// org.springframework.boot.context.embedded.tomcat.TomcatEmbeddedServletContainerFactory
 
@Override
public EmbeddedServletContainer getEmbeddedServletContainer(
        ServletContextInitializer... initializers) {
    Tomcat tomcat = new Tomcat();
    File baseDir = (this.baseDirectory != null) ? this.baseDirectory
            : createTempDir("tomcat");
    tomcat.setBaseDir(baseDir.getAbsolutePath());
    Connector connector = new Connector(this.protocol);
    tomcat.getService().addConnector(connector);
    customizeConnector(connector);
    tomcat.setConnector(connector);
    tomcat.getHost().setAutoDeploy(false);
    configureEngine(tomcat.getEngine());
    for (Connector additionalConnector : this.additionalTomcatConnectors) {
        tomcat.getService().addConnector(additionalConnector);
    }
    prepareContext(tomcat.getHost(), initializers);
    return getTomcatEmbeddedServletContainer(tomcat);
}
 
/**
 * Factory method called to create the {@link TomcatEmbeddedServletContainer}.
 * Subclasses can override this method to return a different
 * {@link TomcatEmbeddedServletContainer} or apply additional processing to the Tomcat
 * server.
 * @param tomcat the Tomcat server.
 * @return a new {@link TomcatEmbeddedServletContainer} instance
 */
protected TomcatEmbeddedServletContainer getTomcatEmbeddedServletContainer(
        Tomcat tomcat) {
    return new TomcatEmbeddedServletContainer(tomcat, getPort() >= 0);
}
```
 
>接下来针对在application.yml 中配置了服务端端口，如何体现到SpringBoot 程序启动过程中的获取到这个配置的端口值的逻辑，来针对性分析SpringBoot 的源码
 
回到上面的TomcatEmbeddedServletContainerFactory.getTomcatEmbeddedServletContainer() 方法中，可以看到是调用TomcatEmbeddedServletContainerFactory.getPort() 获取端口
 
在TomcatEmbeddedServletContainerFactory 可以看到是在构造方法中设置的端口，那就继续看哪里调用TomcatEmbeddedServletContainerFactory 的带有参数的构造方法的？TomcatEmbeddedServletContainerFactory 有多个构造方法，如果没有调用带有端口号的参数，则默认端口号是8080
 
回到在上面的分析过程中，看到是在EmbeddedWebApplicationContext.getEmbeddedServletContainerFactory() 方法中获取EmbeddedServletContainerFactory 的
 
```java
/**
 * Returns the {@link EmbeddedServletContainerFactory} that should be used to create
 * the embedded servlet container. By default this method searches for a suitable bean
 * in the context itself.
 * @return a {@link EmbeddedServletContainerFactory} (never {@code null})
 */
protected EmbeddedServletContainerFactory getEmbeddedServletContainerFactory() {
    // Use bean names so that we don't consider the hierarchy
    String[] beanNames = getBeanFactory()
            .getBeanNamesForType(EmbeddedServletContainerFactory.class);
    if (beanNames.length == 0) {
        throw new ApplicationContextException(
                "Unable to start EmbeddedWebApplicationContext due to missing "
                        + "EmbeddedServletContainerFactory bean.");
    }
    if (beanNames.length > 1) {
        throw new ApplicationContextException(
                "Unable to start EmbeddedWebApplicationContext due to multiple "
                        + "EmbeddedServletContainerFactory beans : "
                        + StringUtils.arrayToCommaDelimitedString(beanNames));
    }
    return getBeanFactory().getBean(beanNames[0],
            EmbeddedServletContainerFactory.class);
}
```
 
可以看到是通过从IoC 容器获取到的实例，说明这个时候TomcatEmbeddedServletContainerFactory 的构造方法已经被调用了
 
现在再回到AbstractApplicationContext 的refresh() 方法，对！**就是那个著名的refresh() 方法！**上面的所有分析都是在onRefresh() 中的逻辑，如下所示可以看到onRefresh() 是在finishBeanFactoryInitialization(beanFactory) 之前执行的，而finishBeanFactoryInitialization(beanFactory) 是对所有的non-lazy-init singletons 进行实例化，但是在onRefresh() 中对于TomcatEmbeddedServletContainerFactory 却是从IoC 容器中获取的，所以TomcatEmbeddedServletContainerFactory 是在什么时候实例化的，它和普通的non-lazy-init singletons 有什么不同？
 
```java
@Override
public void refresh() throws BeansException, IllegalStateException {
    synchronized (this.startupShutdownMonitor) {
        // Prepare this context for refreshing.
        prepareRefresh();
 
        // Tell the subclass to refresh the internal bean factory.
        ConfigurableListableBeanFactory beanFactory = obtainFreshBeanFactory();
 
        // Prepare the bean factory for use in this context.
        prepareBeanFactory(beanFactory);
 
        try {
            // Allows post-processing of the bean factory in context subclasses.
            postProcessBeanFactory(beanFactory);
 
            // Invoke factory processors registered as beans in the context.
            invokeBeanFactoryPostProcessors(beanFactory);
 
            // Register bean processors that intercept bean creation.
            registerBeanPostProcessors(beanFactory);
 
            // Initialize message source for this context.
            initMessageSource();
 
            // Initialize event multicaster for this context.
            initApplicationEventMulticaster();
 
            // Initialize other special beans in specific context subclasses.
            // 上面我们分析的相关逻辑都是在这个函数中执行的，有AbstractApplicationContext 的子类EmbeddedWebApplicationContext 实现！！
            onRefresh();
 
            // Check for listener beans and register them.
            registerListeners();
 
            // Instantiate all remaining (non-lazy-init) singletons.
            // 而普通的non-lazy-init 单例Bean 是在这里实例化的
            finishBeanFactoryInitialization(beanFactory);
 
            // Last step: publish corresponding event.
            finishRefresh();
        }
 
        catch (BeansException ex) {
            if (logger.isWarnEnabled()) {
                logger.warn("Exception encountered during context initialization - " +
                        "cancelling refresh attempt: " + ex);
            }
 
            // Destroy already created singletons to avoid dangling resources.
            destroyBeans();
 
            // Reset 'active' flag.
            cancelRefresh(ex);
 
            // Propagate exception to caller.
            throw ex;
        }
 
        finally {
            // Reset common introspection caches in Spring's core, since we
            // might not ever need metadata for singleton beans anymore...
            resetCommonCaches();
        }
    }
}
```
 
TomcatEmbeddedServletContainerFactory 是在什么时候实例化的，它和普通的non-lazy-init singletons 有什么不同？
 
在TomcatEmbeddedServletContainerFactory 的各个构造方法中加入断点，然后重启应用，看一下什么时候会触发断点，并且看一下TomcatEmbeddedServletContainerFactory 的port 属性是怎么被设置的？！
 
```java
/**
 * Create a new {@link TomcatEmbeddedServletContainerFactory} instance.
 */
public TomcatEmbeddedServletContainerFactory() {
    super();
}
 
/**
 * Create a new {@link TomcatEmbeddedServletContainerFactory} that listens for
 * requests using the specified port.
 * @param port the port to listen on
 */
public TomcatEmbeddedServletContainerFactory(int port) {
    super(port);
}
 
/**
 * Create a new {@link TomcatEmbeddedServletContainerFactory} with the specified
 * context path and port.
 * @param contextPath the root context path
 * @param port the port to listen on
 */
public TomcatEmbeddedServletContainerFactory(String contextPath, int port) {
    super(contextPath, port);
}
```
 
没有在application.yml 配置端口的情况下，走到了TomcatEmbeddedServletContainerFactory() 这个无参构造函数，看一下此时的调用栈
 
```
TomcatEmbeddedServletContainerFactory.<init>() 行： 140    
EmbeddedServletContainerAutoConfiguration$EmbeddedTomcat$$EnhancerBySpringCGLIB$$7700cb2b(EmbeddedServletContainerAutoConfiguration$EmbeddedTomcat).tomcatEmbeddedServletContainerFactory() 行： 79    
EmbeddedServletContainerAutoConfiguration$EmbeddedTomcat$$EnhancerBySpringCGLIB$$7700cb2b.CGLIB$tomcatEmbeddedServletContainerFactory$0() 行： 不可用    
EmbeddedServletContainerAutoConfiguration$EmbeddedTomcat$$EnhancerBySpringCGLIB$$7700cb2b$$FastClassBySpringCGLIB$$af3aaf31.invoke(int, Object, Object[]) 行： 不可用    
MethodProxy.invokeSuper(Object, Object[]) 行： 228    
ConfigurationClassEnhancer$BeanMethodInterceptor.intercept(Object, Method, Object[], MethodProxy) 行： 358    
EmbeddedServletContainerAutoConfiguration$EmbeddedTomcat$$EnhancerBySpringCGLIB$$7700cb2b.tomcatEmbeddedServletContainerFactory() 行： 不可用    
NativeMethodAccessorImpl.invoke0(Method, Object, Object[]) 行： 不可用 [本机方法]    
NativeMethodAccessorImpl.invoke(Object, Object[]) 行： 90    
DelegatingMethodAccessorImpl.invoke(Object, Object[]) 行： 55    
Method.invoke(Object, Object...) 行： 508    
CglibSubclassingInstantiationStrategy(SimpleInstantiationStrategy).instantiate(RootBeanDefinition, String, BeanFactory, Object, Method, Object...) 行： 162    
ConstructorResolver.instantiateUsingFactoryMethod(String, RootBeanDefinition, Object[]) 行： 588    
DefaultListableBeanFactory(AbstractAutowireCapableBeanFactory).instantiateUsingFactoryMethod(String, RootBeanDefinition, Object[]) 行： 1178    
DefaultListableBeanFactory(AbstractAutowireCapableBeanFactory).createBeanInstance(String, RootBeanDefinition, Object[]) 行： 1072    
DefaultListableBeanFactory(AbstractAutowireCapableBeanFactory).doCreateBean(String, RootBeanDefinition, Object[]) 行： 511    
DefaultListableBeanFactory(AbstractAutowireCapableBeanFactory).createBean(String, RootBeanDefinition, Object[]) 行： 481    
AbstractBeanFactory$1.getObject() 行： 312    
DefaultListableBeanFactory(DefaultSingletonBeanRegistry).getSingleton(String, ObjectFactory<？>) 行： 230    
DefaultListableBeanFactory(AbstractBeanFactory).doGetBean(String, Class<T>, Object[], boolean) 行： 308    
DefaultListableBeanFactory(AbstractBeanFactory).getBean(String, Class<T>) 行： 202    
AnnotationConfigEmbeddedWebApplicationContext(EmbeddedWebApplicationContext).getEmbeddedServletContainerFactory() 行： 201    
AnnotationConfigEmbeddedWebApplicationContext(EmbeddedWebApplicationContext).createEmbeddedServletContainer() 行： 164    
AnnotationConfigEmbeddedWebApplicationContext(EmbeddedWebApplicationContext).onRefresh() 行： 136    
AnnotationConfigEmbeddedWebApplicationContext(AbstractApplicationContext).refresh() 行： 537    
AnnotationConfigEmbeddedWebApplicationContext(EmbeddedWebApplicationContext).refresh() 行： 124    
SpringApplication.refresh(ApplicationContext) 行： 693    
SpringApplication.refreshContext(ConfigurableApplicationContext) 行： 360    
SpringApplication.run(String...) 行： 303    
SpringApplication.run(Object[], String[]) 行： 1118    
SpringApplication.run(Object, String...) 行： 1107    
Application.main(String[]) 行： 11    
```
 
通过这个调用栈，很清楚，刚好就是在尝试从IoC 容器中获取TomcatEmbeddedServletContainerFactory 这个Bean 的时候触发了其构造函数的执行
 
这就涉及到一个知识点：在Spring 中，所以的Bean 其实会先被解析为BeanDefinition 放到某个BeanDefinition 容器中，然后等到调用BeanFactory.getBean() 方法时，如果其对应被实例化为Bean 实例了，则直接从Bean 容器中获取，而如果是第一次调用getBean() 方法，则会触发该Bean 的初始化（即调用构造函数进行构造），并将其放到Bean 容器中
 
而上面的分析过程就可以看到其实是刚好第一次getBean() 获取TomcatEmbeddedServletContainerFactory 的时候触发了其构造
 
好的接下来就是找TomcatEmbeddedServletContainerFactory 对应的BeanDefinition 是在什么时候被解析并注册到BeanDefinition 容器中？
 
借助于Eclipse 的强大功能，可以在【选中TomcatEmbeddedServletContainerFactory 之后】-->【鼠标右键】-->【引用】-->【项目】，可以看到其在这里被引用
 
```java
/**
 * {@link EnableAutoConfiguration Auto-configuration} for an embedded servlet containers.
 *
 * @author Phillip Webb
 * @author Dave Syer
 * @author Ivan Sopov
 * @author Stephane Nicoll
 */
@AutoConfigureOrder(Ordered.HIGHEST_PRECEDENCE)
@Configuration
@ConditionalOnWebApplication
@Import(BeanPostProcessorsRegistrar.class)
public class EmbeddedServletContainerAutoConfiguration {
 
    /**
     * Nested configuration if Tomcat is being used.
     */
    @Configuration
    @ConditionalOnClass({ Servlet.class, Tomcat.class })
    @ConditionalOnMissingBean(value = EmbeddedServletContainerFactory.class, search = SearchStrategy.CURRENT)
    public static class EmbeddedTomcat {
 
        @Bean
        public TomcatEmbeddedServletContainerFactory tomcatEmbeddedServletContainerFactory() {
            return new TomcatEmbeddedServletContainerFactory();
        }
    }
 
    /**
     * Nested configuration if Jetty is being used.
     */
    @Configuration
    @ConditionalOnClass({ Servlet.class, Server.class, Loader.class,
            WebAppContext.class })
    @ConditionalOnMissingBean(value = EmbeddedServletContainerFactory.class, search = SearchStrategy.CURRENT)
    public static class EmbeddedJetty {
 
        @Bean
        public JettyEmbeddedServletContainerFactory jettyEmbeddedServletContainerFactory() {
            return new JettyEmbeddedServletContainerFactory();
        }
    }
 
    /**
     * Nested configuration if Undertow is being used.
     */
    @Configuration
    @ConditionalOnClass({ Servlet.class, Undertow.class, SslClientAuthMode.class })
    @ConditionalOnMissingBean(value = EmbeddedServletContainerFactory.class, search = SearchStrategy.CURRENT)
    public static class EmbeddedUndertow {
 
        @Bean
        public UndertowEmbeddedServletContainerFactory undertowEmbeddedServletContainerFactory() {
            return new UndertowEmbeddedServletContainerFactory();
        }
    }
 
    /**
     * Registers a {@link EmbeddedServletContainerCustomizerBeanPostProcessor}. Registered
     * via {@link ImportBeanDefinitionRegistrar} for early registration.
     */
    public static class BeanPostProcessorsRegistrar
            implements ImportBeanDefinitionRegistrar, BeanFactoryAware {
 
        private ConfigurableListableBeanFactory beanFactory;
 
        @Override
        public void setBeanFactory(BeanFactory beanFactory) throws BeansException {
            if (beanFactory instanceof ConfigurableListableBeanFactory) {
                this.beanFactory = (ConfigurableListableBeanFactory) beanFactory;
            }
        }
 
        @Override
        public void registerBeanDefinitions(AnnotationMetadata importingClassMetadata,
                BeanDefinitionRegistry registry) {
            if (this.beanFactory == null) {
                return;
            }
            registerSyntheticBeanIfMissing(registry,
                    "embeddedServletContainerCustomizerBeanPostProcessor",
                    EmbeddedServletContainerCustomizerBeanPostProcessor.class);
            registerSyntheticBeanIfMissing(registry,
                    "errorPageRegistrarBeanPostProcessor",
                    ErrorPageRegistrarBeanPostProcessor.class);
        }
 
        private void registerSyntheticBeanIfMissing(BeanDefinitionRegistry registry,
                String name, Class<?> beanClass) {
            if (ObjectUtils.isEmpty(
                    this.beanFactory.getBeanNamesForType(beanClass, true, false))) {
                RootBeanDefinition beanDefinition = new RootBeanDefinition(beanClass);
                beanDefinition.setSynthetic(true);
                registry.registerBeanDefinition(name, beanDefinition);
            }
        }
    }
 
}
```
 
以上这段代码其实就是所谓的SpringBoot 自动配置的一个典型实现！
 
有过[Spring 基础](http://www.xumenger.com/spring-config-20200603/) 的都知道EmbeddedServletContainerAutoConfiguration 上面有@Configuration 注解，所以其中@Bean 被标注的方法对应的Bean 会被加载到IoC 容器中
 
另外这个配置类上还标注了@Import(BeanPostProcessorsRegistrar.class)，所以它也会把BeanPostProcessorsRegistrar 放入到IoC 容器中，而BeanPostProcessorsRegistrar 实现了ImportBeanDefinitionRegistrar，所以其registerBeanDefinitions() 方法会被回调，然后如代码所示，其会将EmbeddedServletContainerCustomizerBeanPostProcessor、ErrorPageRegistrarBeanPostProcessor 这两个BeanPostProcessor 包装成BeanDefinition 注册到BeanDefinition 容器中
 
>EmbeddedServletContainerCustomizerBeanPostProcessor、ErrorPageRegistrarBeanPostProcessor 可以作为后续切入研究的方向！
 
回到TomcatEmbeddedServletContainerFactory，可以看到是默认的构造方法，那么port 是在哪里设置的呢？
 
在[Spring 构造器循环依赖：constructor-arg 解析](http://www.xumenger.com/spring-bean-circle-20201026/) 介绍循环依赖的时候，我们知道Bean 的属性的设置可能是在构造函数中通过constructor-arg，也可能是通过对应属性的set 方法，从TomcatEmbeddedServletContainerFactory 的继承关系往上找，在父类AbstractConfigurableEmbeddedServletContainer 中有对应的setPort() 方法
 
```java
// org.springframework.boot.context.embedded.AbstractConfigurableEmbeddedServletContainer
@Override
public void setPort(int port) {
    this.port = port;
}
```
 
尝试在这里加断点，继续运行，果然命中了这个断点，此时的调用栈是这样的（上面注入的EmbeddedServletContainerCustomizerBeanPostProcessor，在这个调用栈中也有体现！所以好好理解一下SpringBoot 的自动化配置！）
 
```
TomcatEmbeddedServletContainerFactory(AbstractConfigurableEmbeddedServletContainer).setPort(int) 行： 158    
ServerProperties.customize(ConfigurableEmbeddedServletContainer) 行： 192    
EmbeddedServletContainerCustomizerBeanPostProcessor.postProcessBeforeInitialization(ConfigurableEmbeddedServletContainer) 行： 73    
EmbeddedServletContainerCustomizerBeanPostProcessor.postProcessBeforeInitialization(Object, String) 行： 59    
DefaultListableBeanFactory(AbstractAutowireCapableBeanFactory).applyBeanPostProcessorsBeforeInitialization(Object, String) 行： 407    
DefaultListableBeanFactory(AbstractAutowireCapableBeanFactory).initializeBean(String, Object, RootBeanDefinition) 行： 1623    
DefaultListableBeanFactory(AbstractAutowireCapableBeanFactory).doCreateBean(String, RootBeanDefinition, Object[]) 行： 553    
DefaultListableBeanFactory(AbstractAutowireCapableBeanFactory).createBean(String, RootBeanDefinition, Object[]) 行： 481    
AbstractBeanFactory$1.getObject() 行： 312    
DefaultListableBeanFactory(DefaultSingletonBeanRegistry).getSingleton(String, ObjectFactory<？>) 行： 230    
DefaultListableBeanFactory(AbstractBeanFactory).doGetBean(String, Class<T>, Object[], boolean) 行： 308    
DefaultListableBeanFactory(AbstractBeanFactory).getBean(String, Class<T>) 行： 202    
AnnotationConfigEmbeddedWebApplicationContext(EmbeddedWebApplicationContext).getEmbeddedServletContainerFactory() 行： 201    
AnnotationConfigEmbeddedWebApplicationContext(EmbeddedWebApplicationContext).createEmbeddedServletContainer() 行： 164    
AnnotationConfigEmbeddedWebApplicationContext(EmbeddedWebApplicationContext).onRefresh() 行： 136    
AnnotationConfigEmbeddedWebApplicationContext(AbstractApplicationContext).refresh() 行： 537    
AnnotationConfigEmbeddedWebApplicationContext(EmbeddedWebApplicationContext).refresh() 行： 124    
SpringApplication.refresh(ApplicationContext) 行： 693    
SpringApplication.refreshContext(ConfigurableApplicationContext) 行： 360    
SpringApplication.run(String...) 行： 303    
SpringApplication.run(Object[], String[]) 行： 1118    
SpringApplication.run(Object, String...) 行： 1107    
Application.main(String[]) 行： 11    
```
 
在TomcatEmbeddedServletContainerFactory(AbstractConfigurableEmbeddedServletContainer).setPort(int) 的上一层是ServerProperties.customize(ConfigurableEmbeddedServletContainer)，其相关源码包括
 
```java
@ConfigurationProperties(prefix = "server", ignoreUnknownFields = true)
public class ServerProperties
        implements EmbeddedServletContainerCustomizer, EnvironmentAware, Ordered {
 
    /**
     * Server HTTP port.
     */
    private Integer port;
 
    /**
     * Network address to which the server should bind to.
     */
    private InetAddress address;
 
    // ......
    // 其他属性
 
 
    @Override
    public void customize(ConfigurableEmbeddedServletContainer container) {
        if (getPort() != null) {
            container.setPort(getPort());
        }
        if (getAddress() != null) {
            container.setAddress(getAddress());
        }
        if (getContextPath() != null) {
            container.setContextPath(getContextPath());
        }
 
        // 其他属性设置
        // ......
    }
 
    // 其他方法
    // ......
}
```
 
很明显了，显然就是在application.yml 或者application.properties 中的server 开头的对应配置项！！！！
 
SpringBoot 项目中同时存在application.properties 和application.yml 文件时，两个文件都有效，但application.properties 配置项的优先级会比application.yml 高，可以理解为，先加载application.yml 的配置，然后再加载application.properties 的配置，如果有重复项，后者会覆盖前者
 
>其实之所以开这个话题的研究是因为在我的项目中，在application.yml 中配置了server.port 但是没有生效，就想弄清楚SpringBoot 获取配置的逻辑流程，结果梳理下来，把原理摸清楚了，但是根本原因竟然是同时存在application.properties 和application.yml 导致的（尴尬）
 
>Spring、SpringBoot 是一个太庞大的框架，除了庞大的源码量，还有很多说不清的背景，当初为什么这么设计？这个特性是为了解决什么问题？所以不如就像这样一个小点一个小点的梳理
 
>对于MyBatis、Kafka、Redis、Netty、Spark、ElasticSearch 等开源项目也是一样，直接扎入到源码中，很容易懵掉，就像本文一样，带着一个问题然后去通过逐行调试的方式分析源码，这样一次一次地就把某个大项目的各个小的点各个击破了，过了一段时间，你可能就会发现对整体的架构也摸清楚了

